# 编译期内置插件：芯片协议框架。
# 当前随主程序一起编译，后续 L1 拆分时再改为独立插件包。
[package]
name = "upt.proto"
version = "1.0.0"
plugin_api = 1
kind = "protocol"
layer = "required"
entry = "builtin"
provider = "builtin"

[dependencies]

[permissions]

[capabilities]
