[package]
name = "upt-proto"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1", features = ["derive"] }
upt-chipdb = { path = "../upt-chipdb" }
upt-devices = { path = "../upt-devices" }
