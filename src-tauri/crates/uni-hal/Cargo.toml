[package]
name = "uni-hal"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
uni-plugin = { path = "../uni-plugin" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
