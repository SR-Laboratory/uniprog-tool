# 编译期内置插件：UI WebView 宿主。
# 当前随主程序一起编译，后续 L1 拆分时再改为独立插件包。
[package]
name = "uni.ui.webview"
version = "1.0.0"
plugin_api = 1
kind = "ui"
layer = "required"
entry = "builtin"
provider = "builtin"

[dependencies]

[permissions]

[capabilities]
