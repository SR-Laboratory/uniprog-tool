# 编译期内置插件：硬件抽象层路由。
# HAL 负责能力协商与适配器选择，语义上属于 adapter；当前随主程序一起编译，
# 后续 L1 拆分时再改为独立插件包。
[package]
name = "upt.hal"
version = "1.0.0"
plugin_api = 1
kind = "adapter"
layer = "required"
entry = "builtin"
provider = "builtin"

[dependencies]

[permissions]

[capabilities]
